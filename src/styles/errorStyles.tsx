import { styled } from 'styled-components'

import { Wrapper as GlobalWrapper } from './GlobalStyles'

export const Wrapper = styled(GlobalWrapper)`
  margin: 0 auto;
  text-align: center;
  margin-top: 10rem;
  min-height: auto;
`
